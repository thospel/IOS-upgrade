#!/opt/Shell/tooling/device-apply/bin/device-apply -A

# Check for new IOS

[Include File=IOS.actions.txt]
define: old_file
define: new_file
define: have_file
define: debug
define: verify

# Is the upgrade done ?
[If Not, Regex, FromVariable=new_file]
${old_file}(\.bin|bin|in|n|)

  [Error]
  Not running ${new_file} (instead running ${old_file})

[EndIf]

[If Not,FromVariable=have_file]
1

  [Error]
  Running ${new_file} but the file is not in flash.
[EndIf]
